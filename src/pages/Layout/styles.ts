import { makeStyles } from '@mui/styles';
export const useStyles = makeStyles({
  wrapper: {
    maxWidth: 1080,
    width: '100%',
    marginTop: 50,
    marginBottom: 50,
    background: '#fff',
    borderRadius: 30,
  },
});
