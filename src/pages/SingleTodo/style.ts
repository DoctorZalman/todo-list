import { makeStyles } from '@mui/styles';
// Photos styles
export const useStyles = makeStyles({
  singleContainer: {
    padding: 40,
  },
  singleItem: {
    alignItems: 'center',
    marginTop: 21,
    padding: 14,
    height: 56,
    border: '1px solid',
    borderRadius: 4,
    borderColor: 'rgba(0, 0, 0, 0.3);',
    color: 'black',
    opacity: '0.7',
  },
});
