import { makeStyles } from '@mui/styles';
// Layout Page
export const useStyles = makeStyles({
  wrapper: {
    width: 1080,
    marginTop: 50,
    marginBottom: 50,
    background: '#fff',
    borderRadius: 20,
  },
});
